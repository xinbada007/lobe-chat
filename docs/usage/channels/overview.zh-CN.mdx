---
title: 渠道概览
description: >-
  将 LobeHub 代理连接到外部消息平台，如 Discord、Slack、Telegram、LINE、QQ、微信、飞书和
  Lark；查看 WhatsApp 与 iMessage 的预告状态，让用户可以直接在他们喜欢的聊天应用中与 AI 助手互动。
tags:
  - 渠道
  - 消息渠道
  - 集成
  - Discord
  - Slack
  - Telegram
  - LINE
  - QQ
  - 微信
  - 飞书
  - Lark
  - WhatsApp
  - iMessage
---

# 渠道

**渠道（Channels）适合把一个 Agent 接到你自己的机器人上。** 你先在 Discord、Slack、Telegram 等平台创建或连接机器人，再在 LobeHub 中把它绑定到一个 Agent。之后，用户给这个机器人发消息，就是在与该 Agent 对话。

```text
用户 / 群成员 → 你配置的机器人 → 固定绑定的 Agent
```

## 我该使用渠道还是 Messenger？

- **选择渠道**：你想使用自己的机器人名称和头像，或让同事、客户、群成员使用一个指定的 Agent。
- **选择 [Messenger](/zh/docs/usage/messenger/overview)**：你只想通过 LobeHub 官方机器人使用自己的 Agent，不想创建机器人或管理 Token。

| | **渠道（Channels）** | **Messenger** |
| --- | --- | --- |
| 聊天入口 | 你创建或连接的机器人 | LobeHub 官方机器人或托管服务 |
| Agent 关系 | 每个渠道固定绑定一个 Agent | 每个账号选择一个当前 Agent，可随时切换 |
| 谁来使用 | 你、获准用户、团队或社区成员 | 已关联 LobeHub 账号的用户本人 |
| 配置位置 | **Agent → 渠道** | **设置 → Messenger** |
| 你需要做什么 | 准备平台机器人或应用凭据，并设置访问范围 | 关联平台账号并选择 Agent |

渠道是否对外开放由访问策略决定。你也可以把自己的机器人限制为仅自己或指定用户使用。

> \[!NOTE]
>
> 微信渠道需要个人付费 Plan。按 [2026-07-08 公告](/zh/changelog/2026-07-08-wechat-chat-adjustment)，新建、重连以及预告期结束后的已有连接均需付费 Plan。公告预计于美国东部时间 2026-07-10 12:00 PM（北京时间 2026-07-11 00:00）生效；未满足条件时可能不显示微信选项。

## 支持的平台

### 当前可用

| 平台                                        | 描述                                                    |
| ----------------------------------------- | ----------------------------------------------------- |
| [Discord](/zh/docs/usage/channels/discord)   | 连接到 Discord 服务器，用于频道聊天和私信                             |
| [Slack](/zh/docs/usage/channels/slack)       | 连接到 Slack，用于频道和私信对话                                   |
| [Telegram](/zh/docs/usage/channels/telegram) | 连接到 Telegram，用于私人和群组对话                                |
| [LINE](/zh/docs/usage/channels/line)         | 通过 LINE Messaging API 连接到 LINE，支持私聊和群聊                |
| [QQ](/zh/docs/usage/channels/qq)             | 连接到 QQ，用于群聊和私信                                        |
| [微信](/zh/docs/usage/channels/wechat)         | 通过 iLink Bot 连接微信私聊（Pro；群聊支持未确认）                    |
| [飞书](/zh/docs/usage/channels/feishu)         | 连接到飞书，用于团队协作（中国版）                                     |
| [Lark](/zh/docs/usage/channels/lark)         | 连接到 Lark，用于团队协作（国际版）                                  |

### 即将上线

| 平台                                           | 状态与说明                                              |
| -------------------------------------------- | ------------------------------------------------- |
| [WhatsApp](/zh/docs/usage/channels/whatsapp) | 即将上线；当前不能配置。计划用于移动聊天中的客户支持与跟进。                 |
| [iMessage](/zh/docs/usage/channels/imessage) | 即将上线；当前为占位入口。已有 Labs 说明不代表该频道已正式开放。             |

## 工作原理

每条渠道连接都属于当前 Agent。平台收到消息后，LobeHub 将消息交给这个 Agent，再把回复发送回原对话。

- **一个 Agent 可以连接多个平台** — 例如同时连接 Discord、Slack 和 Telegram。
- **不同 Agent 可以使用不同机器人** — 适合分别服务不同团队、客户或社区。
- **访问范围可以限制** — 支持的平台可分别设置私信、群聊、用户白名单和频道白名单。
- **凭据加密保存** — Bot Token 和应用密钥在存储前会加密。

## 快速开始

1. 前往您的代理设置页面，选择 **渠道** 标签
2. 选择一个当前可用的平台并按照设置指南操作：
   - [Discord](/zh/docs/usage/channels/discord)
   - [Slack](/zh/docs/usage/channels/slack)
   - [Telegram](/zh/docs/usage/channels/telegram)
   - [LINE](/zh/docs/usage/channels/line)
   - [QQ](/zh/docs/usage/channels/qq)
   - [微信](/zh/docs/usage/channels/wechat)
   - [飞书](/zh/docs/usage/channels/feishu)
   - [Lark](/zh/docs/usage/channels/lark)

WhatsApp 与 iMessage 当前显示为**即将上线**，不能按已开放渠道进行配置。可分别查看 [WhatsApp 状态页](/zh/docs/usage/channels/whatsapp)和 [iMessage 预告页](/zh/docs/usage/channels/imessage)。

如果您在渠道列表中看不到 **微信**，请先确认当前个人账号是否符合上述付费 Plan 要求。

## 功能支持

所有平台均支持文本消息。某些功能因平台而异：

| 功能        | Discord | Slack | Telegram | LINE | QQ | 微信 | 飞书   | Lark |
| --------- | ------- | ----- | -------- | ---- | -- | -- | ---- | ---- |
| 文本消息      | 是       | 是     | 是        | 是    | 是  | 是  | 是    | 是    |
| 私人消息      | 是       | 是     | 是        | 是    | 是  | 是  | 是    | 是    |
| 群组聊天      | 是       | 是     | 是        | 是    | 是  | 未确认 | 是    | 是    |
| 表情反应      | 是       | 是     | 是        | 否    | 否  | 否  | 部分支持 | 部分支持 |
| 图片 / 文件附件 | 是       | 是     | 是        | 仅入站  | 是  | 是  | 是    | 是    |

微信群聊支持尚未从仓库资料中确认，本指南仅说明私聊接入。2026-07-08 公告说明的是付费访问调整，未宣布群聊支持。详见[微信渠道](/zh/docs/usage/channels/wechat)。

## 允许的用户（全局）

**高级设置** 里的 **允许的用户 (allowFrom)** 是一道**全局**用户级闸门。填入后，**所有**入站消息（私信和群聊 `@提及` 都算）都必须来自列表里的用户，无论私信策略 / 群组策略选什么都生效。留空则不做用户级过滤，由各 scope 的策略自行决定。

填入后的行为：

| 入口              | 不在白名单的发送者                                                                                  |
| --------------- | ------------------------------------------------------------------------------------------ |
| **私信**          | 被拒绝，对方在私信里收到一条 "您没有私信此机器人的权限" 的系统提示。                                                       |
| **群组 / 频道 @提及** | 被拒绝，会在 @ 所在的同一线程里发一条 "您没有与该机器人交互的权限" 的提示（Discord 上这是 @ 时自动创建的 thread，不会污染父频道；其他平台则可见于该群组）。 |

每行填一个用户：左边是平台用户 ID（必填），右边是可选的 **备注** —— 这个备注只会在设置页展示给你自己，让你日后回看时不用费力辨认 `U01ABCXYZ` 到底是张三还是值班号；运行时完全不读它。

> **防自我锁出**：如果你已经填了 **你的平台用户 ID**（AI 工具字段），那个 ID 会被全局白名单隐式信任。即便你把 bot 收紧到只给朋友、忘了把自己加进 **允许的用户**，也不会被锁出。

## 私信策略

私信策略只影响私信 — 群聊里的 `@提及` 由下面的 **群组策略** 单独管理。全局 **允许的用户** 的用户级过滤也会同时生效；各 scope 的策略叠加在上面。

| 策略                  | 行为                                                                                                                                    |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **开放 (Open)**       | 平台上的任何用户都可以私信机器人（如设置了全局白名单则受其约束）。适合面向所有人开放的助手。                                                                                        |
| **白名单 (Allowlist)** | 私信需要发送者在 **允许的用户** 里。和 `Open` 的差别在白名单为空时：`Allowlist` 模式下会**全部拒绝**，而 `Open` 模式下任何人都能私信。                                                |
| **配对审批 (Pairing)**  | 与 `Allowlist` 共享同一份名单，但非名单用户被拒后会收到一次性配对码，由你（属主）通过 `/approve <code>` 审批。审批通过的用户会被自动追加到 **允许的用户**，后续 DM 直通。需先填 **你的平台用户 ID** 并部署 Redis。 |
| **禁用 (Disabled)**   | 机器人会忽略所有私信。适合那种 " 只在群里被 `@` 时才回复 " 的场景。                                                                                               |

## 群组策略

同样适用于支持群聊的那些平台，每个渠道都有一个 **群组策略 (Group Policy)**，用来控制机器人在群组、频道、子话题里的响应范围。它跟私信策略相互独立：`groupPolicy=disabled` 的机器人仍会接收私信（受私信策略约束），反之亦然。

| 策略                  | 行为                                       |
| ------------------- | ---------------------------------------- |
| **开放 (Open)**       | 在机器人能读取的任何群组、频道、子话题里被 `@` 都响应（默认）。       |
| **白名单 (Allowlist)** | 只有 **允许的频道** 里列出的频道 / 群组 / 会话才会响应，其他都忽略。 |
| **禁用 (Disabled)**   | 忽略所有非私信流量，机器人退化为纯私信模式。                   |

选择 **白名单** 时会出现 **允许的频道** 编辑器：每行填一个平台原生的频道 / 群组 / 会话 ID，可附带一个备注（比如 `#general`），备注只展示给你自己，方便日后辨认每个 ID 是哪个频道。

> **Discord 父频道**：在 Discord 频道里 `@提及` 机器人时，平台会自动开一个 reply thread，入站 thread ID **不等于** Discord「复制频道 ID」给你的那个值。这里直接粘贴**父频道 ID** 就行 —— 它会让父频道下的所有自动 reply thread 都通过白名单。如果你只想放行某个具体 thread，也可以填那个 thread 的 ID，效果就是只放行该 thread。

### 各平台默认值

提供这两项策略的平台默认均为 **开放 (Open)**。如需限制访问，可按渠道改为 **白名单 (Allowlist)** 或 **禁用 (Disabled)**。微信接入按私聊说明，不提供上述两个策略选项；不能据此推断支持微信群聊。

### 如何获取用户的平台 ID

- **Discord** — 在用户设置里开启开发者模式，右键用户头像选 **复制用户 ID**。
- **Slack** — 打开用户资料 → 点击 `⋮` 菜单 → **复制成员 ID**（以 `U` 开头）。
- **Telegram** — 让用户私信 [@userinfobot](https://t.me/userinfobot)，或者从机器人收到的 update 里读 `from.id`。
- **QQ** — 使用 OpenAPI 事件 payload 里的 `openid` —— 私聊（C2C）消息用 `user_openid`，群聊消息用 `member_openid`（用户对外可见的 QQ 号不一定就是平台 ID）。
- **飞书 / Lark** — 使用事件 payload 里的 `open_id`，或开发者后台显示的 **User ID**。

### 如何获取频道 / 群组 ID

- **Discord** — 开启开发者模式，右键频道选 **复制频道 ID**。
- **Slack** — 打开频道详情面板，底部能看到频道 ID（以 `C` 开头）。
- **Telegram** — 把群里的一条消息转发给 [@userinfobot](https://t.me/userinfobot)，或者从机器人收到的 update 里读 `chat.id`（群组是负数）。
- **飞书 / Lark** — 使用事件 payload 里的 `chat_id`。
